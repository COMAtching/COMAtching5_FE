[pr_description]
# 1. 마커 모드 & 제목 자동 생성
use_description_markers = true
generate_title = true

# 2. 👇 [중요] 기능 켜기 (이게 false면 안 나옵니다)
generate_class_diagram = true       # 다이어그램 그리기
enable_semantic_files_type = true   # 파일 목록 표 만들기
enable_pr_type = true               # PR 타입(Enhancement 등) 표시

# 3. 잡담 끄기 (User가 싫어하는 것들)
include_generated_by_header = false # "Generated by..." 배너 삭제
enable_help_text = false            # 본문 도움말 삭제
enable_help_comment = false         # 댓글 도움말 삭제

# 4. 언어 및 배치 지령
language = "ko"
extra_instructions = """
- 답변은 한국어로 작성해주세요.
- '요약' 섹션은 개조식(bullet point)으로 3줄 이내로 작성해주세요.
- [중요] '구현 사항(walkthrough)' 섹션을 작성할 때 다음 내용들을 모두 포함해주세요:
  1. 관련 파일 목록 표 (Relevant files table)
  2. 클래스 다이어그램 (Mermaid class diagram)
  3. 파일별 상세 변경 내용
- 프론트엔드 개발자 톤앤매너를 유지해주세요.
"""

[pr_description.walkthrough]
enable = true

[github_app]
pr_commands = ["/describe"]
handle_pr_actions = ["opened", "reopened"]

[pr_reviewer]
enable_review_labels = false
enable_auto_approval = false